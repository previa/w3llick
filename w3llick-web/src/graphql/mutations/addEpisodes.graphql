mutation AddEpisodes($tmdb_id: Int!) {
  addEpisodes(tmdb_id: $tmdb_id) {
    id
    air_date
    episode_number
    tmdb_id
    name
    overview
    season_number
    still_path
    vote_average
    vote_count
  }
}